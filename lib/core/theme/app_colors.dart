import 'package:flutter/material.dart';

class AppColors {

  /// PRIMARY BRAND
  static const primary = Color(0xFF9C7BFF); // ungu muda
  static const primaryDark = Color(0xFF6A4DFF);

  /// BACKGROUND
  static const background = Color(0xFFF6F5FF);

  /// SURFACE
  static const surface = Colors.white;

  /// DARK AREA
  static const dark = Color(0xFF1E1E2E);

  /// TEXT
  static const textPrimary = Color(0xFF222222);
  static const textSecondary = Color(0xFF777777);
}